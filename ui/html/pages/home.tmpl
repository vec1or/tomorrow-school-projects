{{define "title"}}Home{{end}}

{{define "main"}}
    <h2>Latest Posts</h2>
    {{if .Posts}}
    <table>
        <tr>
            <th>Title</th>
            <th>CreatedAt</th>
            <th>ID</th>
        </tr>
        {{range .Posts}}
        <tr>
            <td><a href='/post/view?id={{.ID}}'>{{.Title}}</a><td>
            <td>{{.CreatedAt}}</td>
            <td>#{{.ID}}</td>
        </tr>
        {{end}}
    </table>
    {{else}}
        <p>There's nothing to see here yet!</p>
    {{end}}
{{end}}